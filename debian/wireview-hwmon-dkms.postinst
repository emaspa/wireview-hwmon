#!/bin/sh
set -e
if [ "$1" = "configure" ]; then
    dkms add -m wireview-hwmon -v 1.2 2>/dev/null || true
    dkms build -m wireview-hwmon -v 1.2 || true
    dkms install -m wireview-hwmon -v 1.2 || true
fi
#DEBHELPER#
