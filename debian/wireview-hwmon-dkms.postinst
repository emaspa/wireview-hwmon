#!/bin/sh
set -e
if [ "$1" = "configure" ]; then
    dkms add -m wireview-hwmon -v 1.3 2>/dev/null || true
    dkms build -m wireview-hwmon -v 1.3 || true
    dkms install -m wireview-hwmon -v 1.3 || true
fi
#DEBHELPER#
