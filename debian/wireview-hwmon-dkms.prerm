#!/bin/sh
set -e
if [ "$1" = "remove" ]; then
    VER=$(grep -m1 '^PACKAGE_VERSION=' /usr/src/wireview-hwmon-*/dkms.conf 2>/dev/null | sed 's/.*=//; s/"//g')
    if [ -n "$VER" ]; then
        dkms remove -m wireview-hwmon -v "$VER" --all 2>/dev/null || true
    fi
fi
#DEBHELPER#
