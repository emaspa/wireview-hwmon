#!/bin/sh
set -e
if [ "$1" = "configure" ]; then
    systemctl daemon-reload || true
    udevadm control --reload-rules || true
    udevadm trigger || true
    deb-systemd-helper enable wireviewd.service || true
    deb-systemd-invoke start wireviewd.service || true
fi
#DEBHELPER#
